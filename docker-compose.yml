services:
  web:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: my_bar.settings
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3
